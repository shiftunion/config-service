{
  "name": "config-admin-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173",
    "test": "vitest run",
    "test:unit": "vitest --run",
    "test:e2e": "playwright test -c playwright.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0",
    "happy-dom": "^18.0.1",
    "typescript": "^5.4.0",
    "vite": "^5.0.0",
    "vitest": "^1.6.0"
  }
}
